<!DOCTYPE html>
<html>
  <head>
    <title>ProductCarousel</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <ul class="nav nav-pills">
        <li role="presentation" class="<%= @active_tab == :preview ? 'active' : '' %>">
          <%= link_to "Preview Carousel", preview_path %>
        </li>
        <li role="presentation" class="<%= @active_tab == :configure ? 'active' : '' %>">
          <%= link_to "Configure Carousel", configure_path %>
        </li>
      </ul>
      <%= yield %>
    </div>
  </body>
</html>
